{
  "name": "dolom",
  "version": "0.1.0",
  "main": "src/main.js",
  "scripts": {
    "build": "rollup -c",
    "watch": "concurrently 'rollup -c -w' 'rollup -c test/rollup.config.js -w'",
    "test": "rollup -c test/rollup.config.js && tape dist/test.js"
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "faucet": "0.0.1",
    "jsdom": "^16.5.0",
    "rollup": "^0.64.1",
    "tape": "^4.9.1"
  },
  "dependencies": {},
  "private": true,
  "xo": {
    "space": 2,
    "globals": [
      "document"
    ]
  }
}
